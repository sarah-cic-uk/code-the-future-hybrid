version: 1
backend:
  phases:
    build:
      commands:
        # Backend lives in amplify-backend/. Deploy the Gen2 backend for this branch
        # and write amplify_outputs.json straight into the frontend folder (public/).
        - cd amplify-backend && npm ci && npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID --outputs-out-dir ../public
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # No frontend build step needed — it's static. The backend phase already wrote
        # public/amplify_outputs.json (via --outputs-out-dir ../public) for the app to use.
        - echo "Static frontend — using public/amplify_outputs.json from the backend phase"
  artifacts:
    baseDirectory: /public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

# Always revalidate HTML/JS/CSS so a deploy is picked up immediately
# (browser may cache, but must check freshness — fast 304s via the CDN).
customHeaders:
  - pattern: '**/*.html'
    headers:
      - key: 'Cache-Control'
        value: 'no-cache'
  - pattern: '**/*.js'
    headers:
      - key: 'Cache-Control'
        value: 'no-cache'
  - pattern: '**/*.css'
    headers:
      - key: 'Cache-Control'
        value: 'no-cache'
  - pattern: '/amplify_outputs.json'
    headers:
      - key: 'Cache-Control'
        value: 'no-cache'

# Made with Bob
